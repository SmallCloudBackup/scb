/**
 * SmallCloudBackup - Application configuration interface
 *
 * This web application provides interface to configure SCB:
 * - sources
 * - transformation and versioning
 * - outputs
 */
var app = require('scb-config');

module.exports = app;
